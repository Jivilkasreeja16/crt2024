read two numbers and print division
#include <stdio.h>

int main()
{
   int a,b; 
   scanf("%d%d",&a,&b);
   if(b==0)
   {
       printf("Second cannot zero");
       b=1;
   }
       int r=a/b;
       printf("\n%d",r);
    return 0;
}
