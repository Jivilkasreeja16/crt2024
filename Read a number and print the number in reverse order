Read a number and print the number in reverse order
#include <stdio.h>
#include<math.h>

int main()
{
    int n,d,r;
    scanf("%d",&n);
    while(n>0)
    {
        d=n%10;
        d=n/10;
        r=r*10+d;
    }
         printf("%d",r);
        

        
        
    

    return 0;
}
